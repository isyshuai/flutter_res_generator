name: flutter_res
description: flutter Assets Indexs
version: 0.0.1
homepage: http://www.baidu.com

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

  source_gen: ^1.0.0
  analyzer: ^1.7.1
  build: ^2.0.2
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.flutter.flutter_res
        pluginClass: FlutterResPlugin
      ios:
        pluginClass: FlutterResPlugin
